(()=>{
  const portValue=value=>{const text=String(value??'').trim();if(!/^\d+$/.test(text))return null;const port=Number(text);return Number.isInteger(port)&&port>0&&port<65536?port:null};
  const hostValue=value=>{const raw=String(value??'').trim();if(!raw||/\s/.test(raw))return null;try{const explicit=/^[a-z][a-z\d+.-]*:\/\//i.test(raw),hostname=explicit?new URL(raw).hostname:raw.replace(/^\[|\]$/g,''),local=hostname==='localhost'||hostname.endsWith('.localhost')||/^\d{1,3}(?:\.\d{1,3}){3}$/.test(hostname)||hostname.includes(':'),url=new URL(explicit?raw:`${local?'http':'https'}://${raw}`);return /^https?:$/.test(url.protocol)&&!url.username&&!url.password&&!url.search&&!url.hash?url:null}catch{return null}};
  function init(){const root=document.querySelector('[data-watchpost-launcher]');if(!root)return;const defaultHost=root.dataset.defaultHost,defaultPort=portValue(root.dataset.defaultPort),hostKey='watchpost-main-host',portKey='watchpost-local-port',form=root.querySelector('[data-config]'),host=form?.querySelector('[name="main-host"]'),port=form?.querySelector('[name="main-port"]'),error=root.querySelector('[data-error]'),saveButton=form?.querySelector('[type="submit"]');const read=()=>{try{return{host:localStorage.getItem(hostKey)||defaultHost,port:portValue(localStorage.getItem(portKey))||defaultPort}}catch{return{host:defaultHost,port:defaultPort}}};const current=()=>{const url=hostValue(host?.value),p=portValue(port?.value);if(!url||!p)return null;url.port=String(p);return{host:host.value.trim(),port:p,url:url.toString()}};const fail=message=>{error.textContent=message;error.hidden=false};const open=target=>window.location.replace(target.url),stored=read(),raw=String(location.search||'').replace(/^\?/,''),params=new URLSearchParams(raw),config=raw==='config'||params.has('config');if(!raw){const url=hostValue(stored.host)||hostValue(defaultHost);url.port=String(stored.port);open({url:url.toString()});return}root.classList.add('config');host.value=stored.host;port.value=String(stored.port);if(!config)fail('Invalid link.');form?.addEventListener('submit',event=>{event.preventDefault();const target=current();if(!target){fail('Enter a valid domain or IP and a port from 1 to 65535.');return}try{localStorage.setItem(hostKey,target.host);localStorage.setItem(portKey,String(target.port))}catch{}error.hidden=true;if(saveButton){saveButton.textContent='Saved';setTimeout(()=>saveButton.textContent='Save',1200)}});root.querySelector('[data-open]')?.addEventListener('click',()=>{const target=current();if(target)open(target);else fail('Enter a valid domain or IP and a port from 1 to 65535.')});root.querySelector('[data-defaults]')?.addEventListener('click',()=>{host.value=defaultHost;port.value=String(defaultPort);try{localStorage.removeItem(hostKey);localStorage.removeItem(portKey)}catch{}error.hidden=true;host.focus()})}
  document.addEventListener('DOMContentLoaded',init);
})();
